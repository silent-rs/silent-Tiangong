# 天工项目规划

## 项目目标

构建可持续运行、可扩展、可治理的个人智能终端，支持桌面、命令行、服务端和远程连接器等使用方式。

## 当前阶段

当前独立分支 `fix/sandbox-stdio-cancellation` 处理 P0“修复沙箱 stdio 中止阻塞”：建立请求级取消、stdio 并发分发、目标进程清理、非阻塞界面命令与可信取消状态，并同步公共 sidecar 协议和官方制品。

分三步完成沙箱能力：command 插件的宿主强制沙箱套壳、macOS 宿主崩溃即时清理和 Windows 文件与网络权限隔离已完成，当前进入逐插件 sidecar 沙箱覆盖；同时保持 Plugin Harness 与 Agent Core 文档和当前代码一致。当前分支只修改宿主、运行时、Launcher 和独立 CI，不改动插件代码或插件清单，不增加兼容标记。

当前 Agent 执行模型是：**空闲用户消息创建 turn task，运行中输入投递到该 task 自有命令通道。** 当前代码不存在常驻 Agent Driver 或 Agent Inbox。

## 当前里程碑

### P0：沙箱 stdio 请求级取消

1. 先统一 Rust/Node stdio 取消协议和请求关联，再让普通请求受并发上限并行处理、取消请求绕过上限。
2. 将会话、工具调用、sidecar 请求和子进程生命周期绑定；只清理目标任务，不中断其他会话或常驻插件。
3. 中止、思考强度切换和长 sidecar 调用不得持有会话锁或占住界面命令处理线程。
4. 前端从停止点击开始保持“正在取消”，直到后端确认目标真正结束；2 秒未确认时显示后台仍运行和重试入口。
5. 取消控制帧 100 毫秒内投递，2 秒内确认接纳；正常进程 2 秒内退出，超时升级强制清理。
6. 旧 sidecar 制品缺少取消协议时明确要求升级，绝不退回无沙箱执行。
7. 完成并发、跨会话、思考强度切换、连续取消、进程残留、三平台、Rust 与前端构建验收后创建单一 PR，未全部完成前不合并。


### Sandbox 通用化、宿主安装目录与生产信任根

1. Sandbox 安装目录由宿主决定，Sandbox 只约定目录内程序名与伴生签名名。
2. 不使用 active、pending 或版本目录表达当前版本。
3. 生产验签只接受 Sandbox crate 内置官方公钥，不接受插件用户密钥或第三方根。
4. 官方验签后执行真实自检并校验协议与策略 Schema。
5. 支持策略 JSON 文件和结构化命令行参数两种通用执行方式。
6. App 接线、旧布局迁移和最低产品版本门槛不在本分支处理。

### 里程碑 0：Command 沙箱执行套壳

1. command 工具每次调用都由宿主创建独立 sidecar，并强制经过随 App 发布的 `tiangong-sandbox` Launcher。
2. 沙箱工作区只取本次工具调用所属会话的权威工作区，不从插件请求或进程级全局状态推导。
3. 每次执行使用独立临时目录，只放行该目录并同步设置 `TMPDIR`、`TMP`、`TEMP`。
4. Launcher 在 macOS、Linux、Windows 上保持可构建；平台隔离能力不可用时明确拒绝执行。
5. 宿主退出或停止调用时清理本次执行的进程组或进程树，并以真实 Launcher 链路验证工作区内写入、工作区外拒绝和临时目录写入。
6. 打包准备使用跨平台工具，并按 Tauri 实际目标三元组选择制品。
7. 插件信任模式、工作区快照恢复、无沙箱审批升级、其他 sidecar 迁移、Windows 完整权限隔离和 Launcher 在线更新不在本分支实现，分别由后续独立分支交付。
8. command 默认禁止读取用户凭据目录；为保证完整 Git、SSH 与 GitHub CLI 工作流，仅允许读取 `~/.ssh` 和 `~/.config/gh`，两处仍禁止写入，其余凭据位置继续禁读，并以受控临时目标真实验证删除、外传、持久化、历史篡改和系统配置写入等危险操作。
9. 清理动态加载等危险环境变量，将临时目录变量固定到本次执行目录，并拒绝符号链接或路径穿越形成的权限逃逸。
10. 命令取消、停止或超时后必须终止整棵进程树；限制单次执行的内存和进程数，并验证并发执行及重复调用不会造成进程、句柄或文件描述符泄漏。
11. 随 App 打包的 Launcher 必须是匹配版本的普通可执行文件，能够实际启动并完成自检；完整 Tauri 安装包必须包含正确目标的 Launcher。
12. 独立 Sandbox CI 在 Linux、macOS、Windows x86_64 上验证可用能力和拒绝边界，覆盖策略一致性、失败诊断，以及 macOS 托管环境允许时的真实 Seatbelt 验证。
13. macOS 在宿主异常退出且 stdio 请求仍在执行时，不等待请求结束或超时，立即终止本次 sidecar 及其进程组。

### 里程碑 1：Windows 完整权限隔离

1. 在现有 Job Object 生命周期与资源限制基础上，增加 Windows 文件系统和网络权限隔离。
2. Windows Launcher 启动的受限目标只能访问策略允许的工作区和本次临时目录，默认不能读取用户凭据或修改工作区 `.git`。
3. 默认拒绝网络；未来需要网络的插件只能由宿主策略明确放行，不能因系统能力不足退回普通进程。
4. Windows x86_64 原生 CI 使用真实 Launcher 验证允许路径、拒绝路径、网络边界、子进程继承、取消、超时和宿主崩溃清理。
5. 完整安装包内的 Launcher 与应用版本匹配，并在目标 Windows 环境实际执行隔离自检。

### 里程碑 2a：Sidecar stdio 传输铺路

1. 插件侧通用库支持 stdio 通道分发：宿主 spawn 注入环境变量选择通道，插件业务代码与清单零改动，不注入时 TCP 行为不变。
2. 宿主侧提供 stdio 连接（懒启动、握手身份校验、请求/进度/通知、崩溃换代重启、进程组清理），工具调用链携带宿主权威会话上下文。
3. 本阶段所有插件保持 TCP 通道、不含 OS 沙箱内容，为逐插件沙箱覆盖与旧制品握手识别提供传输基础；stdio 能力经端到端与手动工具验证。

### 里程碑 2b：解释器形态 Sidecar 与本地信任

1. 插件清单以结构化字段声明解释器 sidecar（runtime 枚举 node/python + entry + 受限 args），不接受命令字符串；解释器由宿主白名单解析，入口为插件目录内子目录脚本。
2. 解释器 sidecar 固定 stdio 通道、无监听端口；自建插件经插件创作链原生确认建立本地信任（内容清单哈希落锚），连接与 spawn 前复核全树、篡改即拒。
3. 交付 Node 协议库与 node-sidecar 创作模板，用户可自建常驻 sidecar 插件；官方签名解释器 sidecar 已支持经官方插件目录分发（见「方向调整」）；Python 协议库与解释器沙箱档由后续阶段交付。

### 里程碑 2：Sidecar 插件沙箱覆盖（后置）

1. 逐个审计所有带 sidecar 的插件，不按插件名称猜测文件、网络、平台接口或传输需求。
2. 基于里程碑 2a 的传输层，支持 stdio 的插件统一使用宿主强制沙箱；由宿主执行真实协议握手识别不兼容制品，不向插件清单增加兼容标记，不支持 stdio 的旧制品必须明确拒绝或完成升级，不静默退回 TCP 裸执行。
3. 网络能力按插件最小需求单独声明；无需网络的插件默认断网，需要网络的插件只在其受控策略中放行。
4. terminal、computer-use、screenshot-input、fs、memory、mcp、scheduler 等特殊插件分别保持 PTY、平台接口、动态路径、流式调用和长连接能力，并建立对应隔离边界。
5. 插件沙箱不可用、策略不完整或能力无法同时满足时明确拒绝执行，不以“已纳入策略”替代真实隔离。
6. 独立 CI 覆盖各插件的启动、调用、拒绝边界、清理和打包制品，确认迁移不破坏存量插件能力。
7. sidecar 的启动时机与运行周期分别管理：需要全局服务能力的存量原生 sidecar 随 App 启动预热并保持；command 继续按调用独立运行；terminal 在首次终端操作时启动，仅在存在 PTY 会话期间保持，最后一个会话结束后退出；解释器仅在首次使用时启动，脚本可按清单选择执行后退出或保持常驻。
8. terminal 与 command 由宿主策略显式获得 `~/.ssh`、`~/.config/gh` 的只读权限及 `~/.cache` 的读写权限，支持 SSH、Git 远程操作、GitHub CLI 与工具缓存；其他 sidecar 不继承这些例外，两个凭据目录在所有沙箱内仍不可写。

### 里程碑 A：Plugin Harness 收敛

1. 统一插件 Slot、Bridge、UI 沙箱和三方 App 贡献模型。
2. 将审批、确认、选择和额外输入统一改为独立的纯 TypeScript Shadow 交互处理器插件：插件负责 `request_user` 工具定义、参数语义、请求编排、2 分钟时限、界面和结果生成；宿主仅提供中立的 TS 工具调用桥接、Shadow 动态加载能力，并提供第三方可直接复制的样板插件。
3. 保持内置界面与插件处理器的契约一致。
4. 完成构建、测试和代码审查后交付当前功能分支。
5. 用输入框截图插件验证 `session.after-input` Slot、会话写入权限和输入草稿附件桥接能力。
6. 修复终端插件输出重复、首屏错位和容器滚动异常，恢复可正常交互的终端显示。
7. 完成浏览器插件管理界面迁移：浏览器页面与终端共同使用 App 拓展区顶部标签，不在插件内部重复维护标签栏，并以原内置浏览器为基准对齐其余界面、状态反馈和既有交互。
8. 优化插件安装与本地导入：只检查和加载目标插件，避免扫描全部已安装插件，并记录关键阶段耗时以定位剩余等待。
9. 收窄 CI 影响范围：公共界面 SDK 变化只检查实际依赖它的界面插件，sidecar 只在自身或公共 sidecar 代码变化时展开跨平台矩阵，无 Bot 改动时不启动 Bot CI。
10. 统一官方 Desktop 插件命名：插件 ID 使用能力名 `terminal`、`interaction`、`browser`，项目目录和工程名统一使用 `tiangong-plugin-*`。
11. 将拓展区是否自动展开收敛为插件 SDK 能力，仅允许声明 `extension.tab` 的 App 插件使用，并由浏览器、终端等插件自行决定展示时机。
12. 修复插件实际调用链：MCP 动态工具名必须可还原到真实 server/tool，浏览器页面可读后立即返回，终端执行命令时在后台创建或复用对应 App 标签且不自动展开拓展区。
13. 发布管道支持纯 UI 插件形态：interaction/browser 无 WASM 与签名清单直接发布，terminal 保持 sidecar 官方签名与三平台矩阵，官方目录新增三个 Desktop 插件条目。
14. 桌面输入框支持输入队列：执行中按 Enter 将当前输入排入会话队列（含附件快照），Cmd/Ctrl+Enter 立即引导当前输入，队列条目可单独引导、发送或移除，turn 结束且草稿为空时自动按序放行队首；队列投递复用现有发送链路（草稿写回 + revision/claim 校验），不做持久化。

### 里程碑 B：Agent Core 文档治理

现行架构说明：

- `docs/agent-loop-refactor/design.md`
- `docs/core-architecture.md`

目标：

1. 文档只描述当前实际存在的 `TiangongCore`、turn task 注册表、per-turn 命令通道和 Agent Loop。
2. 删除已被推翻的常驻 Driver、Agent Inbox和统一常驻命令通道迁移规划。
3. 明确用户消息在空闲和运行中分别走 `start_user_turn` 与 `Command::InjectUserMessage`。
4. 历史方案只从 Git 历史追溯，不再进入当前任务、完成标准和功能讨论。

## 优先级

- P0：沙箱 stdio 的停止必须按请求立即投递并清理目标进程树，不得被长请求、会话锁、界面命令线程或其他会话阻塞。
- P0：后台任务未真正结束前界面不得显示空闲；旧 sidecar 不支持取消协议时必须要求升级，不得关闭沙箱降级。

- P0：command 沙箱不得因平台差异而阻断宿主构建，也不得清理本次执行目录以外的系统临时文件。
- P0：沙箱工作区必须绑定本次工具调用，不能被其他会话、Sub Agent、Bot 或后台任务覆盖。
- P0：平台沙箱不可用、策略非法或目标程序不可信时必须拒绝启动，不能退回无沙箱执行。
- P1：每次 command 执行的临时目录和进程生命周期必须与该次调用一致。
- P1：Launcher 打包与真实链路验证覆盖当前支持的平台和构建目标。
- P0：command 除宿主明确授予的 `~/.ssh`、`~/.config/gh` 只读例外外，不得读取其他用户凭据；凭据目录不得写入，危险环境变量、符号链接和路径穿越不得绕过隔离。
- P0：命令取消、停止或超时后不得遗留子进程；单次执行的内存和进程数量必须受限。
- P1：重复与并发调用不得持续增加宿主文件描述符或句柄，CI 失败时必须保留足够的实际输出和退出状态。
- P1：打包制品必须可执行、版本匹配、不是符号链接，完整 Tauri 安装包必须实际包含 Launcher。
- P0：macOS 宿主异常退出后，正在执行的 sidecar 及其进程树必须立即清理，不得等待业务请求返回。
- P0：所有带 sidecar 的插件必须经过逐项审计并进入明确的沙箱策略；任何例外都必须以隔离能力和真实验证为依据，不能默认裸执行。
- P0：Windows 在文件或网络隔离不可用时必须拒绝启动，不能仅凭进程树清理能力宣称沙箱完整。
- P0：审批征询只收集用户意见并返回 Agent，Core 不解释结果、不保存授权，也不决定 Agent 的后续步骤。
- P0：确认接收的用户消息不得静默丢失。
- P0：工具调用与结果必须保持合法配对。
- P1：插件 Bridge、事件订阅和多实例生命周期保持一致。
- P1：输入辅助插件只能在宿主授权和校验后修改当前输入草稿。
- P1：浏览器插件化不得降低原内置浏览器的可见功能和操作反馈；浏览器页面必须直接进入 App 拓展区顶部标签，与终端混排、切换和关闭。
- P1：单个插件的安装、导入和升级不得重新扫描或加载其他插件；目标插件的校验、切换和失败恢复保持完整。
- P1：CI 必须按实际依赖关系选择任务，禁止界面 SDK 变化触发全部插件 sidecar 的三平台构建。
- P1：官方 Desktop 插件的目录、插件 ID、工程名和宿主引用必须使用统一命名，不保留 `*-handler` 旧标识。
- P1：架构文档必须与当前代码一致，禁止把已删除方案描述为现行设计。
- P2：在行为不下降的前提下继续减少重复的审批与交互处理路径。

## 技术方向

### Sandbox 自管理

- Sandbox 是独立版本化组件，自身持有官方信任根与在线升级能力。
- 宿主存储根只保留 `sandbox/tiangong-sandbox[.exe]` 与伴生签名，不保存版本目录或 active/pending 指针；版本由程序自检报告。首次安装与后续替换复用 Sandbox 自管理能力。
- `check-update` 只检查，`update` 直接替换当前 Sandbox，`update --root` 安装到指定目录；宿主不重复实现升级业务。
- 0.1.0 由官方发布链首次投放，后续版本可直接使用 Sandbox 自身命令升级。
- 首版发布前远端清单允许明确返回 404；宿主仅在本地 Launcher 已通过签名、自检与协议校验时继续使用本地版本。0.1.0 正式发布前的本地策略调整通过重建和重签同版本 Launcher 处理，不额外提升策略 Schema；正式发布后的行为变化通过 Sandbox 产品版本升级交付。
- 发布全局串行、仅主线、版本严格递增，四平台最终制品全部经验签与自检。
- 详细合同见 `docs/sandbox-self-management.md`。

### Command 沙箱

- 宿主在工具调用边界携带会话工作区，command 路由只消费该上下文并忽略插件载荷中的权限声明。
- Launcher 接收宿主通过受控通道传入的版本化策略，验证目标是已安装 command 插件目录内的普通文件后应用操作系统沙箱。
- command 每次调用使用独立临时目录和独立进程生命周期；清理只作用于本次调用创建的文件与进程。
- 沙箱按双分支推进：`feature/sandbox-core` 为独立组件（策略编译、Launcher、签名、独立版本序列、在线更新解析与发布链，合入不改变 App 行为）；`feature/sandbox-app-integration` 承载全部宿主接线（强制路由、设置开关、打包分发、后台更新任务），按用户决定的集成时机合并。
- Launcher 固定安装到存储根的 `sandbox/`，宿主只调用 Sandbox 自管理安装/更新并在启动前逐次验签、自检；环境变量屏蔽清单通过设置页 Modal 管理。

### Sidecar 插件沙箱

- 宿主根据插件经过审计的能力声明构造策略，插件请求不能自行扩大文件、网络或平台权限。
- 传输方式与隔离策略共同设计；stdio、TCP、PTY、流式连接和平台接口分别采用能保持能力且不扩大权限的执行路径。
- terminal 与 command 的 Git 工作流需要读取 SSH 和 GitHub CLI 配置并更新工具缓存，由宿主策略固定开放 `~/.ssh`、`~/.config/gh` 读取及 `~/.cache` 读写；这些例外不来自插件清单，也不扩散到其他 sidecar。
- 插件迁移按单一职责拆分并逐项验证，旧制品不兼容时通过升级或明确拒绝处理。

### Windows 权限隔离

- Job Object 继续负责进程树、宿主退出清理和资源配额，Windows 安全边界负责文件与网络权限。
- Launcher 只有在完整策略成功应用后才启动目标进程，并让子进程继承同一限制。
- Windows 原生运行器是权限隔离的验收环境，非 Windows 主机上的交叉编译只用于提前发现构建问题。

### Agent Core

- `TiangongCore` 负责 Session 加载、上下文构建、空闲起轮和活动期输入路由。
- `shared_runtime` 管理进程级 Tokio runtime 和每个会话当前活跃的 turn task。
- 每个 turn task 持有自己的 `TurnContext` 和命令接收端；task 结束后通道随之失效。
- 运行中的用户消息通过 `Command::InjectUserMessage` 进入当前 turn，按引导消息逻辑中断、保存并重新分析。
- 空闲用户消息通过 `start_user_turn` 从最新 Session 创建新 turn。
- 后续 turn 在真正启动时读取最新 Session，不提前保存未来上下文快照。

### Plugin Harness

- 插件通过 Slot 声明 UI 贡献，通过 Bridge 使用宿主能力。
- Shadow 与 iframe 容器对外提供一致的桥接契约；普通第三方插件可自行声明两者之一，强隔离需求使用 iframe。
- Shadow 脚本可获取自己的根节点、动态宿主上下文并登记卸载回调；会话切换不要求重建需要跨会话保活的插件。
- 审批、选择和额外输入的插件化应建立在当前真实消息路由之上，不引入不存在的 Inbox/Driver 概念。
- Core 与公共插件运行时不得识别 `request_user` 工具名、六种征询类型或其结果文案；这些业务规则属于交互处理器插件。
- 交互处理器返回的是用户意见；Agent 根据普通 Tool Result 决定后续，宿主不建立审批挑战或授权状态。
- 官方 Desktop 插件以能力名作为插件 ID，以 `tiangong-plugin-<能力名>` 作为项目目录和工程名。

## 阶段顺序

当前 P0 按以下顺序交付：

1. 更新需求、规划与任务登记。
2. stdio 并发分发及并发上限。
3. 请求级取消与会话/工具/sidecar 关联。
4. terminal、command 和其他子进程插件清理。
5. 活跃任务立即中止与发送准备阶段重试。
6. 当前会话思考强度更新及锁范围收窄。
7. 长 sidecar 调用移出界面命令处理线程。
8. 前端取消中、后端超时和重试提示。
9. 公共 SDK、官方制品版本与三平台验收。

既有阶段：

1. 收敛 command 沙箱套壳需求和分支边界。
2. 修复平台构建、临时目录、调用级工作区、目标校验和进程清理问题。
3. 改用跨平台打包准备工具并增加真实 Launcher 链路验证。
4. 补齐危险命令、敏感读取、网络、环境、路径、资源、超时、泄漏和并发验证。
5. 验证 Launcher 制品与完整 Tauri 安装包，并收敛三平台诊断结果。
6. 完成完整检查和代码审查后创建 PR。
7. 在当前分支补齐 macOS 宿主崩溃即时清理并完成三平台回归。
8. 从独立分支完成 Windows 文件和网络权限隔离及原生验证。
9. Windows 沙箱完成后，再从独立分支逐插件完成 sidecar 沙箱覆盖与存量制品处理。
10. 插件信任与恢复能力继续在各自独立分支开发。

## 时间节点

- 2026-08-31：启动 P0“修复沙箱 stdio 中止阻塞”，从最新 main 创建单一修复分支。

- 2026-08-24：`feature/sandbox-execution` 收敛为 command 沙箱套壳，开始处理第五轮 Review 问题。
- 2026-08-25：确认沙箱完整能力需继续覆盖 macOS 崩溃即时清理、全部 sidecar 插件与 Windows 文件/网络隔离，拆为三个独立交付项。

- 2026-08-13：完成 Memory 收尾反刍异步入队与验证规划。
- 2026-08-16：完成 Agent Loop 简化及用户引导消息处理；常驻 Driver/Inbox 迁移方案被推翻并从现行架构中移除。
- 2026-08-18：完成 Plugin Harness 主体功能，进入审批/交互接缝和文档收敛阶段。
- 2026-08-18：清理已淘汰 Agent Loop 文档，现行架构以代码和 `docs/agent-loop-refactor/design.md` 为准。
- 2026-08-19：修复终端插件显示链路并交付 Desktop 实际验证。
- 2026-08-20：浏览器插件进入界面与交互对齐阶段；标签归属以 App 拓展区顶部标签为准，插件内部不再保留独立标签栏。
- 2026-08-20：修正浏览器页面完成状态判定，并明确终端 App 关闭后重新打开必须创建新终端。
- 2026-08-20：优化插件安装与本地导入，移除无关插件的全量扫描，并补充安装阶段耗时观测。
- 2026-08-20：优化 PR 的 CI 任务选择，避免公共界面 SDK 和无关 Bot 检查扩散出大量任务。
- 2026-08-20：统一终端、审批征询和嵌入式浏览器插件的目录、插件 ID 与工程名。
- 2026-08-20：发布管道支持无 WASM 的纯 UI 插件，官方目录发布 interaction、browser、terminal 三个 Desktop 插件。
