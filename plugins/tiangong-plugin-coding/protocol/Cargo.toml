[package]
name = "tiangong-plugin-coding-protocol"
version = "0.1.1"
edition = "2024"
license = "Apache-2.0"
publish = false

[package.metadata.tiangong]
business-protocol = 2
description = "Coding WASM 与 sidecar 共用的私有业务协议"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
