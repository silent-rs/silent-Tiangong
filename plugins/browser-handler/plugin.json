{
  "schema_version": 2,
  "id": "browser-handler",
  "version": "0.1.0",
  "entrypoints": [
    "desktop"
  ],
  "permissions": [
    "tool.provide",
    "webview.use",
    "bridge.call"
  ],
  "capabilities": {
    "tools": true,
    "prompt": true,
    "events": [
      "webview.event"
    ]
  },
  "tools": [
    {
      "name": "browser_open",
      "description": "打开浏览器面板并导航到指定 URL（与用户共用同一页面）。",
      "input_schema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "目标 URL"
          }
        },
        "required": [
          "url"
        ]
      },
      "timeout_ms": 30000
    },
    {
      "name": "browser_navigate",
      "description": "在当前浏览器会话中导航到新页面。",
      "input_schema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "目标 URL"
          }
        },
        "required": [
          "url"
        ]
      },
      "timeout_ms": 30000
    },
    {
      "name": "browser_eval",
      "description": "在当前页面执行 JavaScript 并返回结果。",
      "input_schema": {
        "type": "object",
        "properties": {
          "js": {
            "type": "string",
            "description": "要执行的 JavaScript 表达式"
          }
        },
        "required": [
          "js"
        ]
      },
      "timeout_ms": 60000
    },
    {
      "name": "web_fetch",
      "description": "使用内嵌浏览器获取 URL 内容。支持 HTTP/HTTPS 网页和本地 file:// 或绝对路径的 HTML 文件。",
      "input_schema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "目标 URL"
          },
          "max_chars": {
            "type": "integer",
            "description": "内容最大字符数",
            "default": 40000
          },
          "open": {
            "type": "boolean",
            "description": "用户明确要求打开浏览器时为 true",
            "default": false
          }
        },
        "required": [
          "url"
        ]
      },
      "timeout_ms": 300000
    },
    {
      "name": "web_query_dom",
      "description": "在内嵌浏览器当前页面中用 CSS 选择器查询 DOM 元素，返回匹配元素的标签、文本、属性和位置信息。",
      "input_schema": {
        "type": "object",
        "properties": {
          "selector": {
            "type": "string",
            "description": "CSS 选择器"
          },
          "max_results": {
            "type": "integer",
            "description": "最大返回元素数",
            "default": 20
          }
        },
        "required": [
          "selector"
        ]
      },
      "timeout_ms": 60000
    },
    {
      "name": "web_click",
      "description": "在内嵌浏览器当前页面中点击元素（按钮、链接等）。selector 可传 CSS 选择器或自然语言定位描述。",
      "input_schema": {
        "type": "object",
        "properties": {
          "selector": {
            "type": "string",
            "description": "CSS 选择器或自然语言定位"
          }
        },
        "required": [
          "selector"
        ]
      },
      "timeout_ms": 60000
    },
    {
      "name": "web_form_extract",
      "description": "提取内嵌浏览器当前页面中所有表单的字段信息，供 web_form_fill 填写。",
      "input_schema": {
        "type": "object",
        "properties": {}
      },
      "timeout_ms": 60000
    },
    {
      "name": "web_form_fill",
      "description": "在内嵌浏览器当前页面中填写指定表单字段。支持原生 HTML 控件和 UI 库自定义组件。",
      "input_schema": {
        "type": "object",
        "properties": {
          "selector": {
            "type": "string",
            "description": "CSS 选择器或自然语言定位"
          },
          "value": {
            "type": "string",
            "description": "填入的值"
          },
          "strategy": {
            "type": "string",
            "enum": [
              "native",
              "bridge",
              "auto"
            ],
            "default": "auto"
          }
        },
        "required": [
          "selector",
          "value"
        ]
      },
      "timeout_ms": 60000
    },
    {
      "name": "web_locate_element",
      "description": "在内嵌浏览器当前页面中定位元素，返回候选列表。",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "定位描述（自然语言或关键词）"
          }
        },
        "required": [
          "query"
        ]
      },
      "timeout_ms": 60000
    }
  ],
  "prompt": [
    "浏览器工具使用规范：获取网页内容用 web_fetch（用户明确要求打开浏览器时设 open=true）；在当前页面查询元素用 web_query_dom（CSS 选择器）；点击用 web_click；表单先用 web_form_extract 提取再用 web_form_fill 填写；不确定元素位置时用 web_locate_element 定位候选。"
  ],
  "ui": {
    "sandbox": "webview",
    "contributions": [
      {
        "slot": "extension.tab",
        "id": "browser",
        "title": "浏览器",
        "description": "嵌入式浏览器（webview 容器原语 + 插件管理界面）",
        "icon": "globe",
        "entry": "dist/index.html",
        "open_mode": "singleton",
        "context": [
          "session"
        ]
      }
    ]
  }
}
