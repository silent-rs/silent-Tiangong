[package]
name = "tiangong-plugin-generate-image-openai-protocol"
version = "0.2.1"
edition = "2024"
license = "Apache-2.0"

[package.metadata.tiangong]
business-protocol = 1
description = "Generate-Image-OpenAI WASM 与 sidecar 共用的私有业务协议"
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
