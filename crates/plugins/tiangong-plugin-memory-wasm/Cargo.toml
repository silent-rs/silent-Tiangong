[package]
name = "tiangong-plugin-memory-wasm"
version.workspace = true
edition = "2024"
license = "Apache-2.0"
description = "Memory System 的 WASM 桥接组件（经 memory-store request 转发到 sidecar）"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }
serde_json = { workspace = true }
