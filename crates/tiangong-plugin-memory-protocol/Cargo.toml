[package]
name = "tiangong-plugin-memory-protocol"
version.workspace = true
edition = "2024"
license = "Apache-2.0"
description = "Memory 插件私有协议（WASM 与 sidecar 共享，不含运行时依赖）"
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
