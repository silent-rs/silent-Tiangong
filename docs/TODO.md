# 当前任务

- [x] 核对需求，确认沙箱规则错误、证书验证失败和错误丢失。
- [x] 修复沙箱规则、OpenAI 生图连接及错误传递。
- [x] 编译检查及既有失败提示用例通过。
- [x] 经真实 Launcher 沙箱请求现有生图服务，生成图片并打开检查通过。
- [x] 桌面程序、沙箱与图片插件构建完成；尚未替换已安装版本。
- [x] 整理可审查改动，保留在用户指定工作树分支。

## PR #501 审查修复

- [x] 静态 UI 跳过 Yarn 构建，以完整目录跨平台共享；实际生成制品并验证 HTML/CSS/JS 内容、签名条目、校验和及导入完整性。
- [x] 恢复 try_tool_specs/try_prompt_sections 错误传递；Runtime 读取失败检查和 Core 压缩时保留原声明检查通过。
- [x] 恢复停用/暂不可用的明确失败，保留本次工具执行完整错误链；两种状态检查通过。
- [x] 完成 Rust 编译、严格 Clippy、Runtime 库检查（174 项通过、3 项原有 ignored）及新增签名资源检查、Core 声明保留检查、xtask 3 项检查、actionlint 和真实发布/导入验证，整理为 #501 审查修复；未发布或替换本地插件。
