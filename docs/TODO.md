# Runtime 审查修复

- [x] 标题写入成功后才通知界面，失败保留原状态并记录错误；保存成功/失败与压缩结束/取消四种组合验证通过，Core 全部 113 项、严格告警、格式及差异检查通过。
- [x] 修复插件换代与声明读取互相等待。
- [x] 配置内容变化后清理声明缓存，相同配置保留缓存。
- [x] 手动压缩期间接收并保存标题修改。
- [x] 编译、严格告警、格式和差异检查通过；Core 112 项、适配器 3 项验证通过，包含标题重新读取、相同配置保留声明及切换配置清理声明。

# 生图插件恢复 v1（#501）

- [x] 真实沙箱复现旧插件证书格式错误，停止 v2 安装。
- [x] 恢复 v1 清单与 WASM 内嵌设置页，撤回 v2 专用发布改动，保留证书验证修复。
- [x] 编译、严格 Clippy、4 项既有检查、插件结构检查、正式签名验证及完整提交/推送检查通过。
- [x] 已安装恢复后的 v1 插件，模型配置摘要一致，旧制品已备份。真实沙箱内端点可达、过期证书拒绝、禁网拦截均通过。
- [x] 普通图片请求成功生成并归档，已打开确认图像正常；马里奥请求由上游拒绝并返回 image_generation_call failed。桌面控制权限未开放，未验证设置页实际操作。
- [x] #501 已改为恢复 v1 与证书修复范围，未合并或发布。
- [x] 按最新要求升级并安装 0.2.7，以区别原有 0.2.6；本地 v1 清单、真实沙箱程序握手、签名及配置保留均已核验。

- [x] 修复无图片响应丢失上游原因，保留服务错误、拒绝说明和未完成原因并限制错误长度；真实沙箱 0.1.6 中 7 类失败响应检查通过，不输出推理内容。
- [x] 正式签名安装 v1 0.2.8，保留原配置；普通红苹果图片再次真实生成并打开确认，原马里奥请求明确显示服务拒绝理由。编译、严格 Clippy、4 项既有检查与插件结构检查通过。

# 插件退出残留修复（#513）

- [x] 按审查反馈增加 `sh scripts/verify-issue-513.sh` 实进程验证，不增加单元测试。macOS 原生沙箱与信号探测实际通过；32 个业务场景中 30 通过、2 失败、0 环境跳过（另有 2 项构建/环境检查通过）。阻塞 IO 的 4 种 EOF 组合、握手超时、崩溃重启、主动停止及 12 轮后台启动/退出并发均通过。
- [x] 修复实测缺口：macOS 26 对未声明的 signal 默认拒绝，导致宿主正常退出或 SIGKILL 后后台子进程残留。现在只允许同一继承沙箱实例内发信号，覆盖孙进程；普通无关进程和使用相同策略的另一独立沙箱均实测拒绝。保留其他权限边界与旧版 macOS 策略。
- [x] 最终 `sh scripts/verify-issue-513.sh`：33 个业务场景及 2 项构建/环境检查全部通过，0 失败、0 环境跳过；包含 12 轮后台准备/退出并发、阻塞 FIFO、宿主正常退出/强杀、整组回收、握手超时和崩溃重启。日志位于 `target/debug/issue-513-9h2d8bb_`，严格 Clippy、格式与脚本语法检查通过；未新增单元测试，未发布或替换已安装程序。
- [x] 从最新 main 创建独立分支 fix/513-sidecar-stdin-eof，核对 issue 并补充需求。
- [x] 复现并修复 EOF 后被阻塞 IO 拖住的退出缺口：文件插件向无读端 FIFO 追加写入，不声明独占进程组时，旧程序记录 EOF 后仍存活；修复后直接启动约 0.128 秒、真实 fd3 Launcher 沙箱约 0.004 秒退出。保持原有进程组清理，仅补上其未结束当前进程时的退出保证。
- [x] 各平台统一退出标志、预加载/预热/补验证互斥和启动前检查，桌面退出开始即拒绝新启动，避免后台准备晚于清理快照。历史七天残留的原始触发条件尚未重现，不将上述边界复现等同于历史根因。
- [x] 编译、严格 Clippy（含桌面及所有目标）、格式和完整推送检查通过；目录安装检查使用独立完整产物副本并开启必跑，修复共享构建索引缺项造成的环境失败。70b071d9 已推送，PR #514 待审查；macOS 实际验证完成，未发布或替换本机应用及插件。
