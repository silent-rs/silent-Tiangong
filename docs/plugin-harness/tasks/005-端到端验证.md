# T005 - 端到端验证：旧插件经新桥接渲染设置页

## 目标

验证 M0「接缝地基」的完整闭环：旧 WASM 插件不修改即可经新的 Slot + Host Bridge 机制渲染设置页，且现有行为不回退。这是 M0 的验收闸门，不引入新功能。

## 范围

- 验证对象：T001-T004 的产物协同。
- 不改动任何功能代码（仅在发现问题时回改 T002-T004 的 spec 与实现）。

## 依赖

- 前置任务：T004。
- 后续任务：T006（M1 起点）。
- 可并行任务：无。
- 阻塞说明：T004 未完成则无闭环可验。

## 任务

- [ ] 用现有 Memory 插件做完整回归：导入 → 设置页渲染 → 双向通信 → 主题切换。
- [ ] 用纯 WASM 无 sidecar 插件验证兼容路径。
- [ ] 验证 v1 清单插件仍按旧规则解析，设置页正常。
- [ ] 记录验证结果到 `../progress.md`。

## 不做

- 不新增任何 UI/后端功能。
- 不进入 M1（Shadow 容器、能力矩阵）开发。

## 验收

- 旧插件设置页渲染与交互行为与迁移前一致。
- 无回归：工具调用、提示词注入、生命周期不受影响。
- 发现的问题已回改对应任务 spec，并重新通过。

## 验证

- `cargo check` + `yarn --cwd frontend build` + 现有前端测试通过。
- 手动冒烟：Memory 插件设置页可加载、可交互、主题切换正常。
- 回归：现有 `cargo test` / `yarn test` 全绿。
