# 项目规划

## 当前阶段（2026-09-08）

目标：恢复现有 OpenAI 图片生成能力，不新增功能。

优先级与里程碑：定位真实错误 → 修复连接及错误传递 → 编译和实际调用验收。

技术范围：沿用 Rust、WASM 插件及现有 sidecar；不改整体架构。

本次按用户指定在现有工作树分支开发，完成验证后准备 PR 供审查。

## PR #501 审查修复

- 完成静态设置页的共享资源、签名与发布打包，保留既有前端工程构建方式。
- 恢复插件声明读取失败的错误传递，以及停用和暂不可用时的明确失败提示；仅保留本次需要的错误展示改进。
- 验证完整发布制品、历史声明保护和失败原因后更新原 PR，不发布或替换本地插件。
