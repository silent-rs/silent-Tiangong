# Runtime 审查修复

- 在当前 Runtime 重构分支修复换代锁顺序、配置变化后的声明刷新及手动压缩期间标题保存，保持普通轮次声明稳定。
- 手动压缩期间标题写入失败时保留原状态并记录错误，仅在保存成功后通知界面。
- 完成相关 Rust 检查与既有验证后交付审查。

# 当前调整：生图插件恢复 v1（#501）

- 按用户要求恢复 v1 清单及 WASM 内嵌设置页，撤回 v2 静态页面和相关发布改动。
- 修复沙箱内证书验证错误，不扩大权限；按要求升级为 0.2.7 以区分旧版，保留现有模型配置，构建签名后安装本地并验证生图。

- 继续定位“响应未包含图片”：保留并展示上游失败说明，区分模型拒绝与插件/沙箱故障，验证后安装可区分的新版本。

# 插件退出残留修复（#513）

- 目标：stdio 插件随宿主退出，避免进程和内存长期占用。
- 当前阶段优先复现真实沙箱内的退出路径，再修复通用生命周期；沿用现有 Rust 宿主、Launcher 和 sidecar，不增加新的管理体系。
- 修复范围：EOF 后直接结束进程，避免阻塞 IO 拖住运行时关闭；各平台统一退出标志和后台准备互斥，关闭开始后不再启动插件。
- 补齐实测发现的 macOS 沙箱子进程回收权限，只允许向自身和后代发信号；复跑完整 sh 验证，并验证对无关进程仍拒绝，完成后通过微信通知用户。
- 验收：正常关闭输入、强制结束宿主、空闲及请求执行中均不残留；完成相关编译和既有检查后提交 PR 审查。时间以实际验证完成为准。

# 文件与记忆插件独立修复（2026-09-10）

- 从最新 main 独立交付 Fs 0.1.7 与 Memory 0.1.9，消除聊天准备触发的无效后台启动。
- 恢复 Memory 已迁移数据库兼容与可选索引降级，保留原有数据。
- 不包含 App 启动流程或尚未完成的终端沙箱修复。

# PR #521 审查调整：Memory 明文与恢复

- 按最新确认需求取消主数据库加密，历史密钥仅用于读取并转换旧库。
- 优先保证完整备份、转换内容一致和迁移中断可恢复，再修正 Windows 路径降级搜索。
- 基于最新 main 的独立分支承接 #521，沿用 SQLite、LanceDB 与现有插件版本；通过检查后交付审查。

## PR #521 复审：插件历史目录恢复

- 修正空库解密备份与目录恢复检查的冲突，保留原目录备份及其他有效数据保护；验证旧密钥和 v2 密钥的完整恢复与再次启动。
