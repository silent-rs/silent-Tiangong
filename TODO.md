# TODO - 天工 Phase 1 开发任务

> 最后更新：2026-02-13
> 对应 RFC：`docs/rfc/0001-tiangong-desktop-agent-roadmap.md`
> 参考规划：`PLAN.md`

---

## 本阶段目标

完成 Phase 1 基础能力交付，形成可运行的桌面智能助手 MVP：
- 单 Agent 对话可用
- 最小任务执行链路可用
- 工具调用接口可用

---

## 0. 前置与对齐（必须先完成）

- [ ] 确认 Phase 1 范围边界（仅单 Agent、单模型供应商、最小工具集）
- [ ] 定义统一术语与状态机（Task / Step / Run / ToolCall / Result）
- [ ] 定义错误码与失败分级（用户输入错误/模型错误/工具错误/权限拒绝）
- [x] 明确日志与审计字段（时间、会话、任务、工具、结果、耗时）

### 完成标准
- [ ] 核心术语、状态流和错误语义有书面约定
- [ ] 后续模块可复用统一模型，不重复定义

---

## 1. Phase 1 详细能力拆解

### 1.1 桌面 UI（对话与任务视图）

- [x] 提供主界面布局：会话侧栏 + 对话区 + 执行状态区
- [x] 提供固定可见设置入口（侧栏 + 顶栏），可打开模型配置窗口
- [x] 主界面视觉升级为 ChatGPT 风格：深色侧栏 + 顶栏 + 气泡消息 + 底部输入区
- [x] 支持消息输入、发送、流式输出展示（增量渲染且请求期间 UI 不阻塞）
- [x] 支持会话新建、切换、重命名、删除
- [x] 展示任务执行进度（规划中/执行中/完成/失败）
- [ ] 展示工具调用记录与结果摘要

### 1.2 会话与消息模型

- [x] 定义 `Session` / `Message` / `Role` 数据结构
- [x] 实现本地持久化（最小可用：应用重启后会话可恢复）
- [x] 优化持久化粒度：会话切换仅更新 `app.json`，会话文件按需更新
- [x] 优化发送链路落盘策略：单轮请求结束后批量落盘（完成/失败）
- [x] 支持上下文裁剪策略（限制上下文长度，避免无限增长）

### 1.3 模型接入（单供应商）

- [x] 实现 `ModelClient` 抽象与单供应商适配器
- [x] 支持基础参数配置（API_AUTH_TOKEN / API_BASE_URL / API_TIMEOUT_MS / API_MODEL）
- [x] 提供多窗口模型配置页，并支持配置即时生效与本地持久化
- [x] 模型请求改为基于 OpenAI SDK 的真实远程调用（非 mock 输出）
- [x] 设置页支持更新模型列表，并用于 API_MODEL 选择
- [x] 支持流式与非流式返回
- [ ] 实现重试策略（网络错误、429、5xx）
- [x] 记录 token 用量与请求耗时

### 1.4 Planner（MVP）

- [x] 定义最小任务计划结构（单步/多步占位）
- [x] 支持从用户输入生成可执行计划（MVP：先支持单步计划）
- [x] 输出计划可解释信息（为什么这样执行）

### 1.5 Agent Runtime（MVP）

- [x] 定义运行状态机（Idle/Planning/Executing/Completed/Failed）
- [x] 实现任务调度主循环（串行执行优先）
- [ ] 实现中断控制（用户取消当前执行）
- [x] 实现失败回传与状态收敛

### 1.6 Tool Executor（最小工具集）

- [x] 定义统一工具调用协议（入参、出参、错误）
- [x] 实现最小内置工具：
- [x] `read_file`（只读）
- [x] `list_dir`（目录浏览）
- [x] `run_command`（受限命令执行）
- [x] 工具执行结果结构化返回（stdout/stderr/exit_code）

### 1.7 安全基线（Phase 1 必须）

- [ ] API Key 本地安全存储（至少避免明文裸露）
- [ ] 工具权限控制（高风险操作需二次确认）
- [ ] 工作目录隔离与访问边界控制
- [ ] 关键操作审计日志落盘

### 1.8 可观测与诊断

- [ ] 统一日志层（UI、Planner、Runtime、Tool、Model）
- [ ] 支持执行链路追踪 ID（关联一次任务全链路）
- [ ] 提供最小诊断面板（最近错误、最近工具调用）

---

## 2. 交付批次建议（M1/M2/M3）

### M1：可对话
- [ ] 完成 1.1 + 1.2 + 1.3 的最小闭环（无工具执行）

### M2：可执行
- [ ] 完成 1.4 + 1.5 + 1.6，打通“输入 -> 执行 -> 输出”

### M3：可治理
- [ ] 完成 1.7 + 1.8，补齐安全与可观测基线

### 完成标准
- [ ] 用户可通过桌面界面发起任务并拿到可解释结果
- [ ] 工具执行行为可控、可审计、可中断
- [ ] 出现失败时能给出明确错误原因与修复提示

---

## 3. 执行约束

- [ ] 每次改动保持单一职责，优先小步提交
- [ ] 优先按 M1 -> M2 -> M3 顺序推进，避免跨阶段耦合
- [ ] 完成功能后及时回写本文件状态
